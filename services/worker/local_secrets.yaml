# WARNING — intentionally-planted FAKE secret (negative test).
# This is NOT a real credential. It exists only to exercise the gitleaks
# pre-build gate in the `security-scan` CI job: gitleaks (block_on_secrets=true)
# must DETECT this key and FAIL the job, which blocks build-push-api/worker —
# demonstrating that a leaked secret never reaches a built or signed image.
# To turn the pipeline green again, delete this file and push.
aws_access_key_id: AKIAZ9XK2QP7RJ4MN1WD
aws_secret_access_key: wXyZ1aB2cD3eF4gH5iJ6kL7mN8oP9qR0sT1uV2wX
